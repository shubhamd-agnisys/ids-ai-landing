# IDS-AI Website Prototype

Open:

`index-v2.html`

This is a self-contained static prototype. It uses inline SVG, CSS motion, and local JavaScript only. No external assets or product screenshots are required.

The v2 direction is:

- outcome-first hero
- separated proof strip
- "what IDS-AI takes out of the flow" comparison table
- merged natural-language flow section instead of separate flow/thesis/built-for sections
- specification leverage section
- cleaner 1 / 10 / 100 cost graph with source links, a stronger exponential arrow, and broken high-cost bars
- compacted self-correcting loop panels for presentation use
- aligned "Built for complex silicon programs" summary card with motion tokens behind the content
- continuously cycling self-correcting loop while visible
- IDS Collaboration Framework-style prompt-to-files work surface under the loop
- staged IDS Collaboration Framework motion: composer typing -> submitted user prompt -> agent tool replies -> files count to 11
- taller IDS Collaboration Framework panel so the file-generation animation remains visible
- no visible section eyebrow labels
- hero-only background motion with reduced color intensity
- cleaner hero visual: inputs -> IDS-AI -> design, verification, validation, integration, documentation, and sign-off
- hero frame is tighter for presentation; the proof strip intentionally peeks in as the next-scroll cue
- hero motion is stronger inside the hero only; lower sections stay visually quieter
- primary CTA uses a solid Agnisys orange treatment instead of a gradient
- natural-language intent board uses a centered neutral staged trace with UI-font counters, no colored icons, no prompt strip, no packets, and no pulsing cards
- generated-output explorer is now a compact two-column handoff manifest plus active artifact preview
- outcomes are now a compact centered neutral closure ledger under "Closed loops from intent to sign-off."
- capability work surface
- redacted artifact explorer
- calm verified outcomes
- Agnisys substrate explained late

Before production, confirm the approval/apply behavior of IDS-AI and the third-party AI engine positioning. Those claims are intentionally conservative here.
